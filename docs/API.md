# API Documentation

## Auth Endpoints

### POST /api/v1/auth/login
Generate JWT token.

**Body:**
```json
{
  "userName": "admin",
  "password": "admin123"
}
```

**Response:**
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIs..."
}
```

## User Info Endpoints

All endpoints require `Authorization: Bearer <token>` header.

### GET /api/v1/user-infos
List users with optional filter and sort. Returns enriched data with `accountId` (_id from account-login) and `lastLoginDateTime` from account-logins.

**Query Parameters:**
- `fullName` (string, optional) - Filter by full name (case-insensitive regex)
- `role` (string, optional) - Filter by role
- `sort` (string, optional) - Sort field, prefix with `-` for descending
- `page` (number, optional) - Page number, default 1
- `limit` (number, optional) - Items per page, default 10

**Response:**
```json
{
  "data": [
    {
      "_id": "507f1f77bcf86cd799439011",
      "fullName": "Damar Owen",
      "accountNumber": "100000001",
      "emailAddress": "damar@example.com",
      "registrationNumber": "REG-2024-0001",
      "role": "admin",
      "accountId": "507f1f77bcf86cd799439022",
      "lastLoginDateTime": "2026-08-08T10:00:00.000Z"
    }
  ],
  "total": 1
}
```

### GET /api/v1/user-infos/:id
Get user detail by MongoDB `_id`.

### GET /api/v1/user-infos/account-number/:accountNumber
Get user by accountNumber.

### GET /api/v1/user-infos/registration-number/:registrationNumber
Get user by registrationNumber.

### POST /api/v1/user-infos
Create new user + auto-create account login. `_id` is auto-generated by MongoDB.

**Body:**
```json
{
  "fullName": "Damar Owen",
  "accountNumber": "100000001",
  "emailAddress": "damar@example.com",
  "registrationNumber": "REG-2024-0001",
  "role": "user",
  "userName": "damar",
  "password": "password123"
}
```

### PUT /api/v1/user-infos/:id
Update user by MongoDB `_id`. Non-admin can only edit own data.

### DELETE /api/v1/user-infos/:id
Delete user by MongoDB `_id`. Admin only.

## Account Login Endpoints

All endpoints require `Authorization: Bearer <token>` header.

### GET /api/v1/account-logins
List account logins with filter and sort.

### GET /api/v1/account-logins/inactive
Get account logins with lastLoginDateTime older than 3 days.

### GET /api/v1/account-logins/:id
Get account login detail by MongoDB `_id`.

### POST /api/v1/account-logins
Create new account login.

**Body:**
```json
{
  "userName": "damar",
  "password": "password123",
  "userInfoId": "507f1f77bcf86cd799439011"
}
```

### PUT /api/v1/account-logins/:id
Update account login by MongoDB `_id`.

### DELETE /api/v1/account-logins/:id
Delete account login by MongoDB `_id`.
